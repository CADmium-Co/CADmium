{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "serve": "turbo run serve",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^1.13.2"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=20.12.0",
    "pnpm": ">=8.15.5"
  }
}
